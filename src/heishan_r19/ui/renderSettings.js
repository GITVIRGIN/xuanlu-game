export function renderSettings(preferences) {
  return `<section class="settings-screen" data-testid="screen-settings"><div class="settings-panel"><p class="eyebrow">本机设置</p><h2>设置</h2><label><span>自动播放速度</span><select data-action="set-speed" data-testid="setting-speed"><option value="slow" ${preferences.speed === 'slow' ? 'selected' : ''}>慢</option><option value="normal" ${preferences.speed === 'normal' ? 'selected' : ''}>正常</option><option value="fast" ${preferences.speed === 'fast' ? 'selected' : ''}>快</option></select></label><label><span>减少动态效果</span><input type="checkbox" data-action="toggle-motion" data-testid="setting-motion" ${preferences.reduceMotion ? 'checked' : ''}></label><button class="command primary" data-action="go-home">返回首页</button></div></section>`;
}
